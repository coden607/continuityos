services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - continuity-state:/app/.continuity
    restart: unless-stopped

volumes:
  continuity-state:
