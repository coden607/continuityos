name: ci
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python -m pip install -e '.[dev]'
      - run: ruff check packages/python services tests
      - run: python -m compileall -q packages/python services
      - run: pytest -q
      - run: continuity doctor
  pwa:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web-pwa
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm run build
